**Để thêm cột mới vào bảng dữ liệu trong database (MySQL) thì dùng lệnh ALTER TABLE.**

ALTER TABLE ten_bang
ADD COLUMN ten_cot kieu_du_lieu [tùy chọn];

ALTER TABLE users
ADD COLUMN email VARCHAR(100);
